# TaskHome runtime dependencies.
#
# Only direct dependencies are pinned here; transitives (jinja2, werkzeug,
# pillow, qrcode, python-barcode, pyyaml, ...) are resolved by pip.
#
# Rebuild the environment with: ./scripts/setup-venv.sh
# See docs/operations.md for details.

Flask==3.1.1
python-escpos==3.1
pyusb==1.3.1
requests==2.32.5
python-dateutil==2.9.0.post0
